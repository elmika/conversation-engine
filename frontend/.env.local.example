# URL of the FastAPI backend, resolved server-side by Next.js Route Handlers.
# Never exposed to the browser.
# For local dev: http://localhost:8000
# For Docker Compose: http://api:8000  (uses the internal service name)
FASTAPI_URL=http://localhost:8000
